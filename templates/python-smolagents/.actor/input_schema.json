{
  "title": "Python AI agent in Smolagents",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "query": {
      "title": "Query",
      "type": "string",
      "description": "The query for the agent.",
      "editor": "textfield",
      "prefill": "How many seconds would it take for a leopard at full speed to run through Pont des Arts?",
      "default": "How many seconds would it take for a leopard at full speed to run through Pont des Arts?"
    },
    "model": {
      "title": "OpenAI model",
      "type": "string",
      "description": "The OpenAI model to use.",
      "editor": "textfield",
      "default": "gpt-4o",
      "prefill": "gpt-4o"
    }
  },
  "required": ["query", "model"]
}
