{
    "name": "project-langchain",
    "version": "0.0.1",
    "type": "module",
    "description": "This is a boilerplate of an Apify actor.",
    "engines": {
        "node": ">=18.0.0"
    },
    "#_TODO": "Replace the langchain from Git below with an official package once our PR is merged.",
    "dependencies": {
        "apify": "^3.0.0",
        "hnswlib-node": "^1.4.2",
        "langchain": "file:./langchainjs/langchain/",
        "tar": "^6.1.14"
    },
    "devDependencies": {
        "@apify/eslint-config": "^0.3.1",
        "eslint": "^8.36.0"
    },
    "scripts": {
        "start": "node src/main.js",
        "lint": "./node_modules/.bin/eslint ./src --ext .js,.jsx",
        "lint:fix": "./node_modules/.bin/eslint ./src --ext .js,.jsx --fix",
        "test": "echo \"Error: oops, the actor has no tests yet, sad!\" && exit 1"
    },
    "author": "It's not you it's me",
    "license": "ISC"
}
