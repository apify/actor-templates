{
    "title": "LlamaIndex Agent",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "query": {
            "title": "Query to retrieve contact details information from an URL",
            "type": "string",
            "description": "Text query to search for contact details and potentially deduplicate them.",
            "editor": "textfield",
            "prefill": "I would like to get contact details from apify.com"
        },
        "modelName": {
            "title": "OpenAI model (OpenAI is the only supported provider now)",
            "type": "string",
            "description": "Specify the LLM for SQL generation and query synthesis. Currently supports OpenAI models with varying capabilities and performance characteristics.",
            "editor": "select",
            "enum": [
                "gpt-4o-mini",
                "gpt-4o",
                "o1",
                "o1-mini",
                "o3-mini"
            ],
            "default": "gpt-4o-mini"
        },
        "llmProviderApiKey": {
            "title": "LLM Provider API key (OpenAI is the only supported provider now)",
            "type": "string",
            "description": "API key for accessing a Large Language Model.",
            "editor": "textfield",
            "isSecret": true
        }
    },
    "required": [
        "query",
        "llmProviderApiKey"
    ]
}
